name: Generate RSS feeds and deploy to GitHub Pages

on:
  schedule:
    # Every day at 06:00 UTC
    - cron: "0 6 * * *"
  workflow_dispatch: # allow manual trigger from Actions tab
  push:
    branches: [main]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      # ── Cache Rust toolchain + compiled deps ──────────────────────────
      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable

      - name: Build rssify (release)
        run: cargo build --release

      # ── RSS files live in rss/ in the repo; checkout restores them ──

      - name: Generate RSS feeds
        run: ./target/release/rssify -o rss

      # ── Commit RSS files back to the repo so dedup persists ──────────
      - name: Commit RSS feeds
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add rss/
          git diff --cached --quiet || git commit -m "chore: update RSS feeds [skip ci]"
          git push

      # ── Prepare site directory for Pages ─────────────────────────────
      - name: Prepare Pages site
        run: |
          mkdir -p _site
          cp rss/*.xml _site/
          cp pages-index.html _site/index.html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
